storm {
    datasource {
        jdbcUrl = "jdbc:h2:mem:storm-ktor-test;DB_CLOSE_DELAY=-1"
        driverClassName = "org.h2.Driver"
        username = "sa"
        password = ""
        maximumPoolSize = 5
    }
    update {
        defaultMode = "ENTITY"
    }
    validation {
        schemaMode = "none"
    }
}
